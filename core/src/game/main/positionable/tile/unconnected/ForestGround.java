package game.main.positionable.tile.unconnected;

import game.resource.SpriteSheet;

public class ForestGround extends UnconnectedTile {
    public ForestGround() {
        spriteSheet = new SpriteSheet("tiles/forest_ground", 1, 1);
    }
}
